- @anothers.each do |another|
  = link_to item_path(another.id) do
    .item
      .gradient
      .image
        - another.images.first(1).each do |image|
          = image_tag image, class: "item-img"
        .price
          ¥
          = another.price.to_s(:delimited)

        - if another.process == "selltradeing"
          .tag-sold
          .sold SOLD
          
      .detail
        = another.name