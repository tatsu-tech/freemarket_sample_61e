.background
  = render "shared/exhibit-btn"
  = render "shared/header"
  - breadcrumb :logout
  = render "shared/breadcrumbs"

  .mypage-wrapper
    .mypage-logout
      .mypage-logout__box
        .mypage-logout__box--btn
          = link_to destroy_user_session_path, class: "logout-btn", method: :delete do
            .mypage-logout__box--btn--text
              ログアウト
    = render "shared/mypage-nav"

.background
  = render "shared/footer"